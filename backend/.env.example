# JWT Configuration (REQUIRED in production)
# Must be at least 32 characters long
JWT_SECRET=generate-strong-secret-minimum-32-characters

# Environment
NODE_ENV=production

# Server Configuration
PORT=3001
HOST=0.0.0.0

# Frontend URL for CORS
# In production, set this to your actual frontend domain
FRONTEND_URL=https://your-frontend-domain.web.app

# MySQL Database Configuration
# Hostinger VPS: Use DATABASE_URL format for MySQL connection
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://emfudweni_user:your_mysql_password@localhost:3306/emfudweni_school

# OR use individual variables as fallback (if DATABASE_URL is not available)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=emfudweni_user
# DB_PASSWORD=your_mysql_password
# DB_NAME=emfudweni_school
